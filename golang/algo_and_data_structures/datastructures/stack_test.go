package datastructures

import (
	"testing"
	"fmt"
)

func TestStack(t *testing.T) {
	// stackers := CreateStack()

	arr := []int{4, 5}
	fmt.Println(fmt.Sprintf("(%d%d)", ...arr))
// 	if stackers == nil || stackers.size > 0 {
// 		// Error
// 		t.Errorf(`
// Stack failed to initialize`)
// 	}
}