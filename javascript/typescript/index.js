async function test() {
    console.log('qwer');
}
let num;
let font = 'bold';
function pow(x, y) {
    return Math.pow(x, y).toString();
}
const arr = [];
arr.push(1);
const arr2 = [];
class Observable {
    constructor(value) {
        this.value = value;
    }
}
let x;
export {};
